.bundle
.env
.env.*
.git
.gitattributes
.gitignore
.github
.vscode
public/system
public/assets
public/packs
public/packs-test
node_modules
neo4j
vendor/bundle
.DS_Store
*.swp
*~
postgres
postgres14
redis
elasticsearch
chart
storybook-static
.yarn/
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Local development leftovers. Upstream leaves these out of .dockerignore even
# though .gitignore lists them, so a tree that has been developed in ships its
# own caches and test logs inside the image.
tmp
log/*
!log/.keep
coverage
public/packs-dev
.claude
